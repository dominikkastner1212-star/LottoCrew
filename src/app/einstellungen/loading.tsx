import { RouteLoading } from "@/components/route-loading";

export default function Loading() {
  return <RouteLoading title="Admin" description="Profil, Gruppe und Rechte werden geladen." cards={3} />;
}
