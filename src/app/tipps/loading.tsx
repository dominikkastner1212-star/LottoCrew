import { RouteLoading } from "@/components/route-loading";

export default function Loading() {
  return <RouteLoading title="Tipps" description="Tipps und Spielscheine werden geladen." cards={4} />;
}
